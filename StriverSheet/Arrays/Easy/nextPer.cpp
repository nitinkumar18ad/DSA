#include<iostream>
#include<vector>

using namespace std;

vector<int> nextPer(vector<int>& nums) {
    int n = nums.size();
    int index = -1;
    
    for(int i = 0;i<nums.size();i++){
        if(i<n){
            
        }

    }

}